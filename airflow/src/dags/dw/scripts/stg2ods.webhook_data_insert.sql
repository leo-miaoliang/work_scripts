INSERT OVERWRITE table ods.mangguo_webhook_data PARTITION (etl_date='{{ macros.ds(ti) }}')
select  
 `_id`,
 `fs_ip`,
 `channal_detail`,
 `similarity`,
 `user`,
 `synth_content`,
 `is_read`,
 `hd_ct`,
 `is_delete`,
 `finish_at`,
 `answer_at`,
 `label`,
 `call_id`,
 `callback_at`,
 `call_type`,
 `cust_name`,
 `call_at`,
 `host`,
 `queue_at`,
 `channal_status`,
 `info`,
 `cust_number`,
 `number`,
 `cust_tag`,
 `call_status`,
 `full_record_fpath`,
 `voice_type`,
 `call_times`,
 `talk_time`,
 `is_interrupt`,
 `task`,
 `tenant_id`,
 `flow_id`,
 `update_tag_ct`,
 `user_id`,
 `memo`,
 `call_minute`,
 `flow_name`,
 `flow_comment`,
 `task_name`,
 `insert_date`,
 `insert_timestamp`
from stg.mangguo_webhook_data
WHERE etl_date='{{ macros.ds(ti) }}'
;
